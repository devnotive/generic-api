export interface FilterProps {
  search?: string;
  fetchQuery: string;
  countFetchQuery: string;
  date?: string;
  status?: string;
  role?: string;
  communityId?: string;
}
export interface FilterCommunity {
  search?: string;
  fetchQuery: string;
  countFetchQuery: string;
  status?: string;
  schedule?: string;
}
